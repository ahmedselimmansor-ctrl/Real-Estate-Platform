# ---------------------------------------------------------------------------
# search-svc runtime dependencies (Python 3.12)
# Pinned exactly — see requirements-dev.txt for tooling/test dependencies.
# ---------------------------------------------------------------------------

# --- web framework ---------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- validation / settings -------------------------------------------------
pydantic==2.9.2
pydantic-settings==2.6.1

# --- elasticsearch (8.x line, async transport) -----------------------------
elasticsearch[async]==8.15.1

# --- mongodb (read-only: canonical listings are owned by api-core) ---------
motor==3.6.0
pymongo==4.9.2

# --- redis (cache + rate limiting) ----------------------------------------
redis==5.2.1

# --- http client (service-to-service) -------------------------------------
httpx==0.28.1

# --- auth: local HS256 verification of api-core access tokens -------------
PyJWT==2.10.1

# --- serialization / logging / resilience ---------------------------------
orjson==3.10.12
structlog==24.4.0
tenacity==9.0.0
