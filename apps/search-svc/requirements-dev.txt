# ---------------------------------------------------------------------------
# search-svc development / CI dependencies.
#   pip install -r requirements.txt -r requirements-dev.txt
# ---------------------------------------------------------------------------
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.24.0
ruff==0.8.4
mypy==1.13.0
# No types-redis: redis-py ships `py.typed` from 5.0 onward, and the stub
# package stopped at 4.6. Installing it shadows the real types with a version
# that predates `aclose`, so mypy rejects code that runs fine.
