# ---------------------------------------------------------------------------
# apps/rag-svc — runtime dependencies (pinned)
# Python 3.12. Install: pip install -r requirements.txt
# ---------------------------------------------------------------------------

# --- web framework ---------------------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.52.3
sse-starlette==2.2.1
python-multipart==0.0.32

# --- agent / graph runtime -------------------------------------------------
langgraph==0.3.34
langchain-core==0.3.86

# --- config & validation ---------------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.0

# --- persistence -----------------------------------------------------------
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg[binary]==3.2.3
pgvector==0.3.6
alembic==1.14.0
motor==3.6.0
pymongo==4.9.2
redis==5.2.1

# --- http / model providers ------------------------------------------------
httpx==0.28.1
openai==1.59.6
tenacity==9.0.0

# --- text processing -------------------------------------------------------
tiktoken==0.8.0
beautifulsoup4==4.12.3
markdownify==0.14.1
numpy==2.2.1

# --- auth ------------------------------------------------------------------
PyJWT[crypto]==2.13.0

# --- logging / serialization ----------------------------------------------
structlog==24.4.0
orjson==3.11.6
