# Generated by infra/scripts/gen-certs.sh — never commit local TLS material.
# The root .gitignore already covers *.key / *.pem; this covers the whole
# directory so a stray .crt, .csr, .p12 or backup file cannot slip in either.
*
!.gitkeep
!.gitignore
